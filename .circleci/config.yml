version: 2.1

jobs:
  show-secret:
    docker:
      - image: cimg/base:stable
    steps:
      - run:
          name: Print Secret Variable
          command: bash -c 'echo "Secret is ${MY_SECRET}"'

workflows:
  version: 2
  test-secret:
    jobs:
      - show-secret
