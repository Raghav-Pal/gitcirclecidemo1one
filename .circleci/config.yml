version: 2.1

jobs:
  build:
    docker:
      - image: cimg/base:stable
    steps:
      - run:
          name: Build
          command: |
            echo "🔨 Build complete"
            echo "🔐 My secret is $MY_SECRET"

  test:
    docker:
      - image: cimg/base:stable
    steps:
      - run:
          name: Test
          command: |
            echo "🧪 All tests passed"
            echo "🔐 My secret is $MY_SECRET"

  deploy:
    docker:
      - image: cimg/base:stable
    steps:
      - run:
          name: Deploy
          command: |
            echo "🚀 Deploying to production..."
            echo "🔐 My secret is $MY_SECRET"

workflows:
  version: 2
  full-ci-cd:
    jobs:
      - build
      - test:
          requires:
            - build
      - deploy:
          requires:
            - test
